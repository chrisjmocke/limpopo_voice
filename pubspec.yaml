name: limpopo_voice
description: Limpopo Voice App
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^2.27.0
  firebase_auth: ^4.17.0
  cloud_functions: ^4.6.0
  cloud_firestore: ^4.15.0

  flutter_tts: ^3.8.5
  flutter_sound: ^9.2.13
  permission_handler: ^11.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true

  assets:
    - assets/limpopo_voice.png